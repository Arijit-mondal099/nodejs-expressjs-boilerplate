PORT=3000
NODE_ENV=development

# Comma-separated list of origins permitted by CORS.
# Leave empty to reject all browser requests (server-to-server still works).
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
